{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities required by the main Shehata Git window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "clipboard-manager:allow-write-text",
    "dialog:allow-message",
    "dialog:allow-open",
    {
      "identifier": "opener:allow-open-url",
      "comment": "Every address the app opens in a browser must be listed here. An unlisted one is refused with no visible effect, so a button wired to it looks broken rather than blocked.",
      "allow": [
        { "url": "https://github.com/*" },
        { "url": "https://github.com/*/*" },
        { "url": "https://aka.ms/getwinget" }
      ]
    }
  ]
}
